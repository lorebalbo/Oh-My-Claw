---
phase: 04-pdf-classification
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - OhMyClaw/PDF/OpenAIClient.swift
  - OhMyClaw/PDF/PDFTask.swift
  - OhMyClaw/Config/AppConfig.swift
  - OhMyClaw/Resources/default-config.json
  - OhMyClaw/App/AppCoordinator.swift
  - OhMyClaw/App/AppState.swift
  - OhMyClaw/UI/MenuBarView.swift
autonomous: true
requirements: [PDF-02, PDF-03, PDF-04]

must_haves:
  truths:
    - "OpenAIClient sends full PDF text to OpenAI /v1/chat/completions for binary scientific paper classification"
    - "Classification uses configurable model name from pdf.openaiModel config, defaulting to gpt-4o"
    - "Requests use Bearer token auth via pdf.openaiApiKey config"
    - "Requests have 60s timeout with 3 retries and exponential backoff (2s, 4s, 8s)"
    - "After all retries exhausted, PDF is left in Downloads and failure is logged"
    - "Classified papers are moved (not copied) to ~/Documents/Papers, auto-created if needed"
    - "Non-paper PDFs remain untouched in ~/Downloads"
    - "Duplicate filenames in ~/Documents/Papers cause the duplicate in Downloads to be deleted"
    - "API key validation at startup checks pdf.openaiApiKey is non-empty"
    - "Menu bar shows persistent guidance when OpenAI API key is not configured"
    - "PDFTask is registered in AppCoordinator.tasks alongside AudioTask"
    - "Conservative classification: unparseable LLM response -> leave in Downloads"
    - "Single-page PDFs are skipped without calling the LLM (minimum 2 pages)"
  artifacts:
    - path: "OhMyClaw/PDF/OpenAIClient.swift"
      provides: "HTTP client for OpenAI API with classification and retry logic"
      contains: "struct OpenAIClient"
    - path: "OhMyClaw/PDF/PDFTask.swift"
      provides: "Full PDF pipeline conforming to FileTask protocol"
      contains: "struct PDFTask: FileTask"
    - path: "OhMyClaw/Config/AppConfig.swift"
      provides: "PDFConfig with openaiApiKey and openaiModel fields"
      contains: "openaiApiKey"
    - path: "OhMyClaw/Resources/default-config.json"
      provides: "Default openaiApiKey and openaiModel values in pdf section"
      contains: "openaiModel"
    - path: "OhMyClaw/App/AppCoordinator.swift"
      provides: "PDFTask registration and OpenAI API key validation"
      contains: "PDFTask"
    - path: "OhMyClaw/App/AppState.swift"
      provides: "openaiApiKeyConfigured flag for UI binding"
      contains: "openaiApiKeyConfigured"
    - path: "OhMyClaw/UI/MenuBarView.swift"
      provides: "Persistent OpenAI API key guidance message when not configured"
      contains: "openaiApiKeyConfigured"
  key_links:
    - from: "PDFTask.canHandle"
      to: "PDFFileIdentifier.isRecognizedPDFFile"
      via: "File routing delegation"
      pattern: "identifier\\.isRecognizedPDFFile"
    - from: "PDFTask.process"
      to: "PDFTextExtractor.extract"
      via: "Text extraction before classification"
      pattern: "textExtractor\\.extract"
    - from: "PDFTask.process"
      to: "OpenAIClient.classify"
      via: "LLM classification with retry"
      pattern: "classifyWithRetry"
    - from: "PDFTask.process"
      to: "FileManager.moveItem"
      via: "Paper routing to ~/Documents/Papers"
      pattern: "moveItem"
    - from: "AppCoordinator.start"
      to: "OpenAIClient API key check"
      via: "Validate API key is configured at launch"
      pattern: "openaiApiKey\\.isEmpty"
    - from: "AppCoordinator"
      to: "AppState.openaiApiKeyConfigured"
      via: "API key validation updates UI state"
      pattern: "openaiApiKeyConfigured"
    - from: "MenuBarView"
      to: "AppState.openaiApiKeyConfigured"
      via: "Conditional persistent guidance message"
      pattern: "openaiApiKeyConfigured"
---

<objective>
Wire the full PDF classification pipeline: OpenAI HTTP client with Bearer auth and retry logic, PDFTask conforming to FileTask with minimum page filter, config extension for API key and model, AppCoordinator integration with API key validation, and menu bar guidance for missing API key.

Purpose: Complete the PDF classification story end-to-end — scientific papers dropped in ~/Downloads are classified via the OpenAI API (GPT-4o) and moved to ~/Documents/Papers. Non-papers are left untouched. Missing API key is communicated clearly to the user.
Output: Two new files (OpenAIClient.swift, PDFTask.swift), five modified files completing the full PDF pipeline with UI integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pdf-classification/04-RESEARCH.md
@.planning/phases/04-pdf-classification/04-CONTEXT.md
@.planning/phases/04-pdf-classification/04-01-SUMMARY.md
@OhMyClaw/PDF/PDFFileIdentifier.swift
@OhMyClaw/PDF/PDFTextExtractor.swift
@OhMyClaw/Audio/AudioTask.swift
@OhMyClaw/Audio/FFmpegService.swift
@OhMyClaw/App/AppCoordinator.swift
@OhMyClaw/App/AppState.swift
@OhMyClaw/UI/MenuBarView.swift
@OhMyClaw/Config/AppConfig.swift
@OhMyClaw/Resources/default-config.json
@OhMyClaw/Core/Protocols/FileTask.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenAIClient & Config updates</name>
  <files>OhMyClaw/PDF/OpenAIClient.swift, OhMyClaw/Config/AppConfig.swift, OhMyClaw/Resources/default-config.json</files>
  <action>
**Create `OhMyClaw/PDF/OpenAIClient.swift`:**

**Imports:** Foundation

**OpenAIError enum** (Error, Sendable):
- `.missingApiKey` — API key not configured
- `.badResponse(statusCode: Int)` — non-200 HTTP status
- `.emptyResponse` — choices array empty or nil content
- `.decodingFailed(String)` — JSON decode error

**API Codable models** (all Sendable):

`ChatCompletionRequest: Encodable`:
- `let model: String`
- `let messages: [ChatMessage]`
- `let temperature: Double`
- `let max_tokens: Int`
- Nested `ChatMessage: Encodable` with `role: String`, `content: String`

`ChatCompletionResponse: Decodable`:
- `let choices: [Choice]`
- Nested `Choice: Decodable` with `message: Message`
- Nested `Message: Decodable` with `content: String`

`ClassificationResult: Decodable`:
- `let is_paper: Bool`

**OpenAIClient struct** (Sendable):
- Properties: `let apiKey: String`, `let modelName: String`, `let timeout: TimeInterval`
- `private static let baseURL = URL(string: "https://api.openai.com/v1")!`
- `init(apiKey: String, modelName: String, timeout: TimeInterval = 60)`:
  - Store apiKey, modelName, and timeout

**Classification — `func classify(text: String) async throws -> Bool`:**
- Guard `!apiKey.isEmpty` else throw `.missingApiKey`
- Build URL: `Self.baseURL.appendingPathComponent("chat/completions")`
- System prompt (designed for binary classification):
  ```
  You are a document classifier. Determine whether the following document \
  is a scientific or academic paper.

  Respond with ONLY a JSON object: {"is_paper": true} or {"is_paper": false}. \
  No other text.

  Classify as a scientific paper ONLY if the document contains most of these: \
  an abstract, numbered sections, citations in the text, a references/bibliography \
  section, and author affiliations with research institutions or universities.

  Do NOT classify as a paper: GitHub issues, bug reports, invoices, receipts, \
  manuals, product docs, legal documents, forms, newsletters, blog posts, \
  slide decks, or general technical documentation.

  If you are unsure, respond {"is_paper": false}.
  ```
- User content: full extracted text
- Build `ChatCompletionRequest` with `temperature: 0.0`, `max_tokens: 50`
- Encode body via JSONEncoder
- Build URLRequest: POST, Content-Type application/json, Authorization: Bearer <apiKey>, httpBody, timeoutInterval = timeout
- `let (data, response) = try await URLSession.shared.data(for: request)`
- Guard HTTPURLResponse statusCode == 200, else throw `.badResponse(statusCode:)`
- Decode `ChatCompletionResponse`, guard `choices.first?.message.content`, else throw `.emptyResponse`
- Return `parseClassification(content)`

**Parse helper — `private func parseClassification(_ response: String) -> Bool`:**
Conservative: only return true when clearly positive.
- Trim + lowercase the response
- Try JSON decode as `ClassificationResult` first — return `is_paper` value
- Fallback: check for string patterns `"is_paper": true` or `"is_paper":true`
- Anything else: return false (conservative — better to miss a paper than misfile a receipt)

**Retry wrapper — `static func classifyWithRetry(text:client:maxRetries:) async -> Bool?`:**
- `let backoffSeconds: [UInt64] = [2, 4, 8]`
- Loop `0...maxRetries` (default maxRetries = 3, so 4 total attempts):
  - `try await client.classify(text:)` — return result on success
  - On error: log warning with attempt number and error
  - If not last attempt: `try? await Task.sleep(nanoseconds: backoffSeconds[attempt] * 1_000_000_000)`
- After all retries exhausted: return nil (caller treats nil as "leave in Downloads")

Key constraints:
- Use `Task.sleep` not `Thread.sleep` (cooperative suspension)
- 60s timeout per request (cloud API may be slower than local inference)
- Temperature 0.0 for deterministic classification
- max_tokens 50 — only need `{"is_paper": true/false}`
- Bearer token authentication

**Modify `OhMyClaw/Config/AppConfig.swift`:**
Replace `lmStudioPort` and `modelName` in `PDFConfig`:
- Replace `var lmStudioPort: Int` with `var openaiApiKey: String`
- Replace `var modelName: String` with `var openaiModel: String`
- Update `static let defaults` to include `openaiApiKey: ""`, `openaiModel: "gpt-4o"`

**Modify `OhMyClaw/Resources/default-config.json`:**
Replace `"lmStudioPort": 1234` and `"modelName": ""` with `"openaiApiKey": ""` and `"openaiModel": "gpt-4o"` in the `pdf` section.
  </action>
  <verify>
Build the project: `xcodebuild build -project OhMyClaw.xcodeproj -scheme OhMyClaw -destination 'platform=macOS' 2>&1 | tail -5` compiles with zero errors. Verify OpenAIClient.swift has `classify`, `classifyWithRetry`, `parseClassification`. Verify AppConfig.swift PDFConfig has `openaiApiKey` and `openaiModel`. Verify default-config.json has `openaiApiKey` and `openaiModel`.
  </verify>
  <done>OpenAIClient provides binary classification (POST to OpenAI /v1/chat/completions with Bearer auth), and retry with exponential backoff (2s, 4s, 8s). PDFConfig includes openaiApiKey and openaiModel. Response parsing is conservative — only explicit true triggers paper classification.</done>
</task>

<task type="auto">
  <name>Task 2: PDFTask pipeline, AppCoordinator wiring, AppState flag & MenuBarView guidance</name>
  <files>OhMyClaw/PDF/PDFTask.swift, OhMyClaw/App/AppCoordinator.swift, OhMyClaw/App/AppState.swift, OhMyClaw/UI/MenuBarView.swift</files>
  <action>
**Create `OhMyClaw/PDF/PDFTask.swift`:**

**Imports:** Foundation

**PDFTask struct** (FileTask, Sendable):
- `let id = "pdf"`
- `let displayName = "PDF Classification"`
- `let isEnabled: Bool`
- `private static let minimumPaperPages = 2`
- Private dependencies: `identifier: PDFFileIdentifier`, `textExtractor: PDFTextExtractor`, `client: OpenAIClient`, `destinationPath: String`
- Init: accept all dependencies, set `isEnabled` from a parameter

**canHandle(file:) -> Bool:**
- Delegate to `identifier.isRecognizedPDFFile(file)`

**process(file:) async throws -> TaskResult:**

Step 1 — Extract text and metadata:
- `guard let extraction = textExtractor.extract(from: file)` else:
  - Log warning: "No text extractable from PDF" with filename context
  - Return `.skipped(reason: "No extractable text (image-only or password-protected)")`

Step 2 — Minimum page count check:
- `if extraction.pageCount < Self.minimumPaperPages`:
  - Return `.skipped(reason: "Only \(extraction.pageCount) page(s) — not a paper")`

Step 3 — Classify via OpenAI with retries:
- Call `OpenAIClient.classifyWithRetry(text: extraction.text, client: client, maxRetries: 3)`
- If result is nil (all retries failed):
  - Log error: "Classification failed after all retries" with filename
  - Return `.skipped(reason: "Classification failed — leaving in Downloads")`

Step 4 — Check classification result:
- `guard isPaper else`: return `.skipped(reason: "Not classified as a scientific paper")`

Step 5 — Move to ~/Documents/Papers:
- Expand tilde in `destinationPath`: `NSString(string: destinationPath).expandingTildeInPath`
- Build destination directory URL
- `try FileManager.default.createDirectory(at: destDir, withIntermediateDirectories: true)` — auto-create on first paper
- Build file destination: `destDir.appendingPathComponent(file.lastPathComponent)`
- Duplicate check: if file already exists at destination, delete source from Downloads and return `.processed(action: "Duplicate deleted")`
- `try FileManager.default.moveItem(at: file, to: destination)` — move, not copy
- Log info: "Paper moved to ~/Documents/Papers"
- Return `.processed(action: "Classified as paper, moved to ~/Documents/Papers")`

**Modify `OhMyClaw/App/AppState.swift`:**
Add property after `ffmpegAvailable`:
- `var openaiApiKeyConfigured: Bool = true`
- Default true — updated at launch after API key validation

**Modify `OhMyClaw/App/AppCoordinator.swift`:**

In `start()`, after the AudioTask registration block and BEFORE `startMonitoring()`, add PDF pipeline wiring:

1. Read PDF config: `let pdfConfig = store.config.pdf`
2. Create OpenAIClient: `let openaiClient = OpenAIClient(apiKey: pdfConfig.openaiApiKey, modelName: pdfConfig.openaiModel)`
3. Check API key is configured: `let apiKeyConfigured = !pdfConfig.openaiApiKey.isEmpty`
4. Update state: `appState.openaiApiKeyConfigured = apiKeyConfigured`
5. Log outcome:
   - Configured: `AppLogger.shared.info("OpenAI API key configured", context: ["model": pdfConfig.openaiModel])`
   - Not configured: `AppLogger.shared.warn("OpenAI API key not configured — add your API key to config.json in the pdf.openaiApiKey field")`
6. Create PDFTask: `let pdfTask = PDFTask(identifier: PDFFileIdentifier(), textExtractor: PDFTextExtractor(), client: openaiClient, destinationPath: pdfConfig.destinationPath, isEnabled: pdfConfig.enabled)`
7. If `pdfConfig.enabled`: `tasks.append(pdfTask)`
8. Log: `AppLogger.shared.info("PDF pipeline ready", context: ["enabled": "\(pdfConfig.enabled)"])`

No health polling needed — OpenAI is a cloud service. Just validate the API key is present.

**Modify `OhMyClaw/UI/MenuBarView.swift`:**

After the ffmpeg guidance section and BEFORE the final Divider + Quit button, add OpenAI API key guidance:

```swift
if !coordinator.appState.openaiApiKeyConfigured {
    Divider()
    VStack(alignment: .leading, spacing: 4) {
        Label("OpenAI API key not configured", systemImage: "exclamationmark.triangle.fill")
            .foregroundStyle(.orange)
        Text("Add your API key to config.json (pdf.openaiApiKey).")
            .font(.caption)
            .foregroundStyle(.secondary)
        Text("PDF classification is paused until configured.")
            .font(.caption)
            .foregroundStyle(.secondary)
    }
}
```

This mirrors the ffmpeg guidance pattern.
  </action>
  <verify>
Build the project: `xcodebuild build -project OhMyClaw.xcodeproj -scheme OhMyClaw -destination 'platform=macOS' 2>&1 | tail -5` compiles with zero errors. All existing tests still pass. Verify PDFTask conforms to FileTask with id "pdf". Verify AppCoordinator creates OpenAIClient and registers PDFTask. Verify AppState has openaiApiKeyConfigured. Verify MenuBarView shows API key guidance when not configured.
  </verify>
  <done>PDFTask implements complete pipeline: extract -> page check -> classify (with retry) -> route. AppCoordinator wires OpenAIClient, validates API key at launch. MenuBarView shows persistent guidance when API key is missing. Non-papers and failures leave PDFs in Downloads.</done>
</task>

</tasks>

<verification>
- Full project builds with zero errors
- All existing tests still pass
- PDFTask.canHandle identifies PDFs via PDFFileIdentifier
- PDFTask.process: extract -> page check -> classify with retry -> move or skip
- OpenAIClient.classify sends POST to OpenAI /v1/chat/completions with Bearer auth
- Classification response parsed conservatively — only explicit true = paper
- 60s request timeout, 3 retries with 2s/4s/8s exponential backoff
- Failed classification after retries -> PDF stays in ~/Downloads, failure logged
- Papers moved to ~/Documents/Papers (auto-created)
- Duplicate filenames in Papers -> duplicate deleted from Downloads
- Password-protected / image-only PDFs -> skipped with logged warning
- Single-page PDFs -> skipped without calling LLM
- Non-paper PDFs -> left in ~/Downloads untouched
- AppCoordinator registers PDFTask alongside AudioTask
- API key validation at launch — checks pdf.openaiApiKey is non-empty
- MenuBarView shows API key guidance (orange warning) when not configured
- PDFConfig has openaiApiKey and openaiModel in AppConfig and default-config.json
</verification>

<success_criteria>
The full PDF classification pipeline is wired end-to-end: scientific paper PDFs dropped in ~/Downloads are detected, their text extracted via PDFKit, classified via the OpenAI API (GPT-4o), and moved to ~/Documents/Papers. Non-papers remain untouched. Missing API key produces a visible menu bar message. All requirements PDF-02, PDF-03, PDF-04 are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pdf-classification/04-02-SUMMARY.md`
</output>
