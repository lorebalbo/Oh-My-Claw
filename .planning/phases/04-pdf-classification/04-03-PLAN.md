---
phase: 04-pdf-classification
plan: "03"
type: execute
wave: 1
depends_on: ["04-02"]
gap_closure: true
files_modified:
  - OhMyClaw/PDF/LMStudioClient.swift
  - OhMyClaw/PDF/PDFTask.swift
autonomous: true
requirements: [PDF-04]

must_haves:
  truths:
    - "Non-paper PDFs (invoice, receipt, GitHub issue, manual) are left in ~/Downloads untouched"
    - "Duplicate papers already in ~/Documents/Papers are deleted from ~/Downloads, not left there"
  artifacts:
    - path: "OhMyClaw/PDF/LMStudioClient.swift"
      provides: "Improved system prompt with negative examples and structural cues"
      contains: "invoice|receipt|bug report|abstract|references|methodology"
    - path: "OhMyClaw/PDF/PDFTask.swift"
      provides: "Duplicate deletion instead of skip"
      contains: "removeItem|Deleted duplicate"
---

<objective>
Fix two diagnosed UAT gaps in the PDF classification pipeline:
1. (Major) LLM misclassifies non-paper PDFs as papers — strengthen system prompt with explicit negative examples and structural cues
2. (Minor) Duplicate papers are left in ~/Downloads instead of being deleted — change skip to delete
</objective>

<tasks>
<task type="auto">
  <name>Strengthen LMStudioClient system prompt</name>
  <files>OhMyClaw/PDF/LMStudioClient.swift</files>
  <action>
Replace the `systemPrompt` string in `LMStudioClient.classify()` (around line 119) with an improved version that:

1. Keeps the existing instruction format (respond with JSON only, binary classification)
2. Adds a "NOT a scientific paper" section with explicit negative examples:
   - GitHub issues, bug reports, changelogs, release notes
   - Invoices, receipts, purchase orders, financial statements
   - Product manuals, user guides, API documentation
   - Legal documents, contracts, forms
   - Newsletters, marketing materials, slide decks
3. Adds structural cues the LLM should look for before classifying as a paper:
   - Abstract or summary section
   - References or bibliography section
   - Methodology or methods section
   - Author affiliations with universities or research institutions
   - DOI, journal name, or conference proceedings header
4. Strengthens the conservative default: "When in doubt, or if fewer than two of the structural cues are present, respond false"

The new prompt should replace the existing `systemPrompt` let binding. Keep it concise — small local LLMs perform worse with excessively long prompts. Target ~200 words max.
  </action>
  <verify>
  - Build the project: `xcodebuild -scheme OhMyClaw -configuration Debug build 2>&amp;1 | tail -5`
  - Read the modified file and confirm:
    - Negative examples are present (invoice, receipt, GitHub issue, bug report, manual)
    - Structural cues are present (abstract, references, methodology, affiliation)
    - Conservative default is strengthened
    - Prompt is not excessively long
  </verify>
  <done>System prompt contains explicit negative examples, structural cues, and a strengthened conservative default. Project builds without errors.</done>
</task>

<task type="auto">
  <name>Delete duplicate PDFs from Downloads</name>
  <files>OhMyClaw/PDF/PDFTask.swift</files>
  <action>
In `PDFTask.process()`, locate the duplicate check guard block (around line 77):

```swift
guard !FileManager.default.fileExists(atPath: destination.path) else {
    return .skipped(reason: "File already exists in Papers")
}
```

Replace it with logic that:
1. Checks if the file already exists at the destination
2. If it does, deletes the source file from ~/Downloads using `FileManager.default.removeItem(at: file)`
3. Logs the deletion: `AppLogger.shared.info("Deleted duplicate PDF from Downloads", context: ["file": file.lastPathComponent])`
4. Returns `.processed(action: "Duplicate deleted from Downloads — already in Papers")`

This ensures duplicate papers are cleaned up from Downloads while the original in Papers remains unchanged.
  </action>
  <verify>
  - Build the project: `xcodebuild -scheme OhMyClaw -configuration Debug build 2>&amp;1 | tail -5`
  - Read the modified file and confirm:
    - `removeItem(at: file)` is called when duplicate exists
    - A log entry is written for the deletion
    - Return value is `.processed` not `.skipped`
  </verify>
  <done>Duplicate PDFs are deleted from ~/Downloads when the same file already exists in ~/Documents/Papers. Deletion is logged. Return is `.processed`.</done>
</task>
</tasks>

<verification>
- Project builds without errors after both changes
- LMStudioClient system prompt explicitly lists non-paper document types (invoices, receipts, GitHub issues, bug reports, manuals)
- LMStudioClient system prompt lists structural cues for papers (abstract, references, methodology, affiliation)
- LMStudioClient system prompt has strengthened "when in doubt, respond false" instruction
- PDFTask deletes duplicate files from Downloads instead of leaving them
- PDFTask logs duplicate deletions for auditability
- PDFTask returns .processed for duplicate deletions
</verification>

<success_criteria>
Both UAT gaps are closed: the system prompt is precise enough to reject non-paper PDFs (GitHub issues, invoices, receipts, manuals), and duplicate papers are deleted from ~/Downloads instead of being left in place. The project builds cleanly.
</success_criteria>
